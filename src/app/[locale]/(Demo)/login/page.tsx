import LoginPageTemplate from '@/components/template/LoginPageTemplate';

const Page = () => {
  return (
    <div>
      <LoginPageTemplate />
    </div>
  );
};

export default Page;

// className="flex flex-col bg-gradient-to-t"
